'use client'; export function SortSelect({ value, base }: { value: string; base: string }) { return (<select defaultValue={value} aria-label="Järjesta" onChange={(e) => { window.location.href = '/tookojad?' + base + 'sort=' + e.target.value; }}><option value="rec">Soovituslik</option><option value="az">Nime järgi (A–Z)</option><option value="rate">Hinnatuimad</option></select>); }
